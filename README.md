REFLECTION:

1. I handled them by using the optional property operator.
2. When designing the interfaces I used optionals which made the components more flexible. 
3. To ensure type safety, I defining interfaces for components to use those interfaces.
4. I encountered a few challenges when implementing component composition incluging for instance in ProfileCard when calling the onEdit prop in button. The method call was not working, however I managed to fix it. 